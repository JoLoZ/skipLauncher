stages:
  - build

build:
  image: electronuserland/builder:wine
  stage: build
  script:
    - npm
    - npm run dist
  artifacts:
    expire_in: 30 days
    paths:
      - ./dist/
  only:
    - main